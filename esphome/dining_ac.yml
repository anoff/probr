substitutions:
  devicename: dining_ac
  label: Dining (AC)
  mount_room: dining
  mount_floor: "2"
  mount_height: "2.0" # in m

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

<<: !include .template_ac_w_receiver.yml
